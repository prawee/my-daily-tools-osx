# Remove ngrok with `NPM` and using with `Brew` instead

## Remove
```bash
npm uninstall -g ngrok
```

## Install with Homebrew
```bash
arch -arm64 brew install ngrok
```

## Checking
```bash
ngrok version
# ngrok version 3.19.0
```
