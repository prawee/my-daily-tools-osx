# How to create project with CLI

## Make zone
```sh
cd code
mkdir tambo
```

## Create project
```sh
npm create tambo-app
```

```sh
? What is the name of your app? (use "." to create in current directory✔ tambo-first

Creating a new Tambo app in /Users/prawee.won/code/tambo/tambo-first
```

```sh
? Select a template for your new app:
❯ standard - Tambo + Tools + MCP (recommended)
  analytics - Generative UI Analytics Template
```
```sh
✔ Select a template for your new app: standard - Tambo + Tools + MCP (recommended)
Selected template: standard
✔ standard template downloaded successfully
✔ Dependencies installed successfully

Successfully created a new Tambo app
Template: standard - Tambo + Tools + MCP (recommended)
```

```sh
Next steps:
  1. cd tambo-first
  2. git init
  3. npx tambo init to complete setup
  4. npm run dev

Learn More:
  • Each component in your template comes with built-in documentation and examples
  • Visit our UI showcase at https://ui.tambo.co to explore and learn about the components included in your template
```

```sh
cd tambo-first
git init
npx tambo init
```
```sh
Initializing tambo. Choose hosting and let's set up your API key.

? Choose where to connect your app:
  Cloud (time: 1 minute) — recommended
❯ Self-host (time: 5-10 minutes)
```
