# Installation CLI of Nuxt.js

## Required

```bash
node --version
# v14.9.0
npm --version
# 6.14.7
```

## Installation

```bash
npm install -g create-nuxt-app
```

## Test

```bash
create-nuxt-app
# create-nuxt-app v3.2.0
# Can't create . because there's already a non-empty directory . existing in path.
```