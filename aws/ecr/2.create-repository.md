# How to create `Repository` of ECR

```
aws ecr create-repository --repository-name bun-aws-docker
```

## with profile
```
aws ecr create-repository --repository-name bun-aws-docker --profile brt
```
