# How to create app with LoopBack

## Create app
```bash
$ lb4
? Project name: lb4-start
? Project description: (www) leanning loopback v4
? Project root directory: (lb-4-start)lb4-start
? Application class name: (Lb4StartApplication) StartApplication
? Select features to enable in the project (Press <space> to select. <a> to toggle all, <i> to invert selection) <a> <space> to service => Enable tslint, Enable prettier, Enable mocha, Enable loopbackBuild, Enable vscode, Enable repositories, Enable services
```
## Start Application
```bash
$ cd lb4-startß
$ npm start
#Server is running at http://127.0.0.1:3000
#Try http://127.0.0.1:3000/ping
```